class Solution{
    public static String redirectionUrl(String language){
        // 関数を完成させてください
        String url = "www.example.org/";
        if (language == "English") url += "en";
        if (language == "Japanese") url += "ja";
        if (language == "Spanish") url += "es";
        if (language == "Russian") url += "ru";
        if (language == "German");
        return url;
    }
}